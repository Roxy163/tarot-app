# 2026-09-17 等价重构验收记录

本轮以开始清理时的工作区为基线，保留此前已完成的修复。业务源码净减少 681 行（新增 55、删除 736，不含测试、配置和依赖锁文件）。

## 已完成的清理

- 删除没有任何引用或运行入口的 `AvatarCropModal`、`DesignerSlot`、`SpreadGridControls`、`useDarkMode`。当前账号页、牌阵编辑器和主题逻辑继续使用原实现。
- 删除自由画布内无调用入口的旧旋转、缩放、复制、镜像回调及其辅助函数；去掉从表单经工作台传到画布、但从未调用的 `onRemoveSlot` 属性。现有删除通过 `onUpdateSlots` 更新并进入撤销历史。已保存卡位的旋转和缩放展示仍保留。
- 将三处导出的重复下载操作集中到 `src/lib/downloadFile.ts`，保留文件名、内容、类型和下载后的资源释放。
- 将四个表单里完全相同的移动端聚焦滚动集中到 `src/lib/mobileFocus.ts`，保留 768px 边界和 120ms 延迟。登录页的 140ms 与全局键盘适配的 180ms 仍独立保留。
- 合并数组、对象的备份读取流程：依次读取主记录、最新镜像、上一版本；跳过损坏和类型错误的内容；恢复写入失败时仍返回可用备份。写入失败上报和迁移保护不变。
- 统一单张与整套牌义的合并逻辑，保留空文本、空关键词、自定义理解、删除标记和未知卡片的默认值。批量读取直接传入官方牌义，不增加逐张搜索。
- 删除 5 项未使用的直接依赖：`konva`、`react-konva`、`react-easy-crop`、`use-image`、`zustand`；连同间接依赖净移除 10 个包。将原有 `@types/react@19.2.15` 明确列为开发依赖，其他包的锁定版本未变。
- 开启 TypeScript 未使用变量、参数检查，防止新的无效回调继续积累。

## 保留的保护逻辑

数据库规则、Firebase 数据读写、登录上下文、同步协调、手记/日运存储、草稿、日期处理、Service Worker 均与本轮开始时逐文件比对一致。牌义服务中的账号迁移、删除标记、并发同步保护及写入失败处理未修改。600ms 长按、撤销历史、正逆位入口、官方牌阵恢复和保存坐标的核心流程保留。

## 验收证据

- 全量测试：70 个文件，410 项通过；本轮新增 8 项，覆盖备份恢复受限和牌义合并边界。
- 类型检查通过，包括新启用的未使用代码检查。
- 生产构建通过；PWA 资源版本为 `0e5dcf6adf30`。
- 最终数据相关复验：18 项通过；变更格式检查通过。
- 使用本轮开始前的源码副本做差分对照：4,096 组备份读取组合（包括损坏、空值、类型不符、读写被拒绝）返回值、最终存储内容和读写顺序全部一致；324 个牌义读取结果及用户数据导出与重构前完全一致。对照日志为 `output/refactor/equivalence.log`。
- 从应用入口追踪模块引用后，无未使用的业务模块；测试初始化文件由测试配置引用。此检查不代表所有公开导出都已精简。
- 独立访客会话、本机预览验收：390×844 下草稿跨页面和刷新恢复、日期、凯尔特十字布局无横向溢出；自由画布新增、拖动、删除、撤销、保存通过；逆位与解读保存后重新打开核对一致。
- 1440×1000 桌面典籍检查通过；实际下载 PDF、CSV、Markdown，核对文件名、日期、问题和正逆位。浏览器控制台错误和警告均为 0。
- 截图、导出样本位于 `output/playwright/refactor/`；检查日志位于 `output/refactor/`。这些均为本机临时验收产物。

## 后续修复：逐牌解读导出

CSV/Markdown 原先只导出总体解读，遗漏 `cardInterpretations` 中的逐牌解读。现已为 CSV 增加“逐牌解读”和“牌面疑问”字段，并在 Markdown 中增加对应章节；没有内容时明确显示“未填写”。CSV 同时对可能被表格软件识别为公式的用户输入加了安全前缀。

修复后已完成定向测试、全量测试和真实下载验收。测试访客手记中的逐牌解读在保存、重新编辑、CSV 和 Markdown 四处内容一致。

## 生产发布

2026-09-17 已发布到 Cloudflare Pages 生产环境：

- 主站：`https://tarot-pavilion.pages.dev/`
- 部署地址：`https://1bf1086e.tarot-pavilion.pages.dev/`
- PWA 缓存版本：`7e4ba09daa0e`
- 全量结果：70 个测试文件、411 项测试通过；类型检查和生产构建通过。
- 主站和部署地址均返回 200，主站 HTML 与本地 `dist/index.html` 一致；线上 `PrivateTab` 资源与本地产物 SHA-256 一致。
- 生产浏览器进入应用主页后控制台为 0 错误、0 警告。
- 本次没有修改 Firestore Rules，因此没有重复发布数据库规则。

登录、云端同步和权限边界本轮以既有自动化测试及代码比对验证，未操作线上账号或真实云端数据。上述结果不等同于对所有场景的绝对安全保证。
