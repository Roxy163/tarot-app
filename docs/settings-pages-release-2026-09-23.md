# 设置与辅助页面发布记录

2026-09-23 发布，2026-09-24 完成正式网站浏览器验收。先发布已验收的设置与辅助流程，剩余核心研习页面另行改造。

## 发布内容

- 侧栏保留账号与同步卡片，次要入口集中到独立设置页；返回来源页面时保留未完成手记等状态。
- 支持与反馈、安装指引、隐私条款、账号安全和密码辅助流程改为完整页面，保留逐层返回、输入及提交保护。
- 反馈仅保留产品建议、bug 反馈、其他；附件支持图片、PDF、UTF-8 TXT，前后端复用类型、内容格式及大小检查。
- 同步卡片明确本机保存状态，同步说明按需展开，错误及重试仍可见。

## 版本

- 正式网站：https://tarot-pavilion.pages.dev/
- 本次部署：https://e7e6a4cb.tarot-pavilion.pages.dev/
- 应用代码提交：`4a47cce`，已推送 `main`。
- PWA 缓存版本：`6e5177fa8cc3`。
- 前一版部署：https://db63e258.tarot-pavilion.pages.dev/ ，PWA `69373c4f4182`。
- Firestore 规则未改动，本轮不需要发布数据库规则；未修改启动和 Service Worker 更新策略。

## 验收结果

- 全量 77 个测试文件、486 项测试通过；类型检查、生产构建及 Cloudflare Functions 打包通过。
- 正式域名与本次部署地址各检查 22 个资源，均返回 200，内容哈希与本地构建一致；检查到预期的 `nosniff`、`DENY` 响应头。
- 云端反馈接口对空反馈、不支持的 SVG 和伪装 PDF 均返回 400，未触发真实邮件发送。
- 线上浏览器 390×844、1280×900：设置页、反馈完整页面、附件入口、反馈草稿返回保留、安装提醒开关保存、隐私/协议切换均通过。
- 浏览器返回、页首返回、Escape 和退出动画后的焦点恢复、背景恢复可操作性通过；手机尺寸无横向溢出。
- 控制台无错误或警告。测试浏览器的反馈草稿已清空，提醒开关已恢复。
- 截图：`output/playwright/production-feedback-mobile-20260924.png`、`output/playwright/production-feedback-desktop-20260924.png`。
- 发布前日志与资源核验结果：`/private/tmp/tarot-release-20260923-*.log`、`/private/tmp/tarot-release-20260923-verification.json`（临时本机证据，不入库）。

未实际发送反馈或密码重置邮件，也未修改密码、注销账号；这些提交逻辑沿用既有实现，并由自动化测试覆盖。浏览器手机尺寸验收不等同于实体手机启动测试。

## 后续范围

日运记录/补写、牌义详情/注疏编辑、牌阵工作台、日运复盘/小考档案及手记详情仍待独立页面化；需要分别验证草稿、保存失败、来源位置、撤销与画布坐标。删除、未保存确认、选牌和筛选等短操作继续保留弹窗。见 [页面与弹窗交互检查](page-and-dialog-review-2026-09-23.md)。
